## 本地备份 (01:53)
- Git commit：成功（10 个文件变更）
- 备份同步：失败（Z 盘为 SMB 网络共享，robocopy 报错 50：不支持文件属性操作）
- 变更文件：10 个（268 插入，16 删除）

## 本地备份 (13:53)
- Git commit：成功（12 个文件变更，952 插入）
- 备份同步：失败（Z 盘 robocopy ERROR 50：不支持文件属性操作）
- 变更文件：12 个

## 压缩前自检 (2026-05-31 06:00:27)
- memory 目录: 110 个文件, 369.9 KB
- heartbeat-state.json: 存在
- 会话锁: 1
- Z 盘: 已挂载
- Ollama 模型: NAME                       ID              SIZE      MODIFIED     ...
- 注意: 此条由脚本自动记录，非 LLM 生成

## 压缩前自检 (2026-05-31 14:00:42)
- memory 目录: 111 个文件, 372.8 KB
- heartbeat-state.json: 存在
- 会话锁: 1
- Z 盘: 已挂载
- Ollama 模型: NAME                       ID              SIZE      MODIFIED     ...
- 注意: 此条由脚本自动记录，非 LLM 生成
#!/usr/bin/env node
// Fix TTS: create speak.ps1 to avoid $ escaping issues
const fs = require('fs');

// Create PS1 file
const psContent = `
param([string]$Text)
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$voice = New-Object -ComObject SAPI.SpVoice
$voice.Speak($Text, 1)
[Console]::OutputEncoding = [System.Text.Encoding]::WindowsANSI
`;

fs.writeFileSync('C:/Users/48856/.openclaw/workspace/matrix-bridge/speak.ps1', psContent, 'utf8');

// Also fix bot.js speak() function
const botCode = fs.readFileSync('C:/Users/48856/.openclaw/workspace/matrix-bridge/bot.js', 'utf8');
const start = botCode.indexOf('// === TTS（语音输出）===');
const end = botCode.indexOf('// === STT（语音输入）===');

const newSpeak = `// === TTS（语音输出）===
function speak(text) {
    if (!CFG.ttsEnabled) return;
    try {
        const safeText = text.replace(/'/g, "\\'\\'").substring(0, 400);
        cp.execFileSync('powershell.exe', ['-NoProfile', '-ExecutionPolicy', 'Bypass', '-File', path.join(DIR, 'speak.ps1'), '-Text', '"' + safeText.replace(/"/g, '\\"') + '"'], { timeout: 10000, stdio: 'ignore' });
        log('TTS: ' + text.substring(0, 40));
    } catch (e) {
        log('TTS 失败: ' + e.message);
    }
}

`;

const newCode = botCode.substring(0, start) + newSpeak + botCode.substring(end);
fs.writeFileSync('C:/Users/48856/.openclaw/workspace/matrix-bridge/bot.js', newCode, 'utf8');
console.log('TTS fixed! bot.js rewritten.');
# Create speak.ps1 and fix bot.js TTS
$content = @'
param([string]$Text)
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$voice = New-Object -ComObject SAPI.SpVoice
$voice.Speak($Text, 1)
[Console]::OutputEncoding = [System.Text.Encoding]::WindowsANSI
'@
$content | Out-File -FilePath "C:\Users\48856\.openclaw\workspace\matrix-bridge\speak.ps1" -Encoding utf8
Write-Host "speak.ps1 created"
# Create speak.ps1 to avoid $ escaping issues
$psContent = @'
param([string]$Text)
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$voice = New-Object -ComObject SAPI.SpVoice
$voice.Speak($Text, 1)
[Console]::OutputEncoding = [System.Text.Encoding]::WindowsANSI
'@
$psContent | Out-File -FilePath "C:\Users\48856\.openclaw\workspace\matrix-bridge\speak.ps1" -Encoding utf8
Write-Host "speak.ps1 created: $($psContent.Length) bytes"
