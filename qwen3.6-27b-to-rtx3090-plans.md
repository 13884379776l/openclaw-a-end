# 🔥 Qwen3.6:27b 塞进 RTX 3090 方案

> 生成时间：2026-05-06 19:55 GMT+8

---

## 📊 硬件现状

| 资源 | 规格 |
|---|---|
| **RTX 5070 Ti** | 16GB 显存（已用 14.3GB，剩 1.3GB） |
| **RTX 3090** | 24GB 显存（空闲 ✅） |
| **系统内存** | 64GB (4×16GB DDR5 5467MHz) |
| **当前模型** | `qwen3.6:27b` — 17GB (Ollama 量化版) |
| **当前上下文** | OLLAMA_NUM_CTX=16384 |

---

## 🎯 问题

Ollama 默认会优先使用 GPU 0（RTX 5070 Ti），导致 27B 模型塞进 16GB 卡，剩余显存紧张，甚至可能溢出到系统内存拖慢推理速度。

**目标：让 qwen3.6:27b 跑在 RTX 3090（24GB）上，获得完整 GPU 加速。**

---

## 📋 方案清单

### 方案 A：`CUDA_VISIBLE_DEVICES` 指定 GPU（⭐ 推荐）

**原理：** 通过环境变量限制 Ollama 只看到 RTX 3090，强制模型加载到 24GB 卡上。

**步骤：**
1. 确认 3090 是 GPU 1（`nvidia-smi` 已验证 ✅）
2. 设置 `CUDA_VISIBLE_DEVICES=1` 后启动 Ollama
3. 模型 17GB 全部进入 3090 显存，剩余 ~7GB 给 KV cache 和计算

**显存分配预估：**
```
┌─────────────────────────────┐
│  RTX 3090 (24GB)           │
│  ├── 模型权重 ~17GB        │
│  ├── KV cache ~3-4GB       │
│  └── 剩余 ~3-4GB ✅        │
└─────────────────────────────┘
```

**配置方式（选择一种）：**
- **方式 1：** 环境变量（永久生效）
- **方式 2：** 修改 Ollama 启动脚本/服务
- **方式 3：** PowerShell 临时测试

**优点：** 简单、模型完整加载、推理速度最快
**缺点：** 5070 Ti 无法被 Ollama 使用（需保持 14GB 占用则无影响）
**风险：** 低

---

### 方案 B：下载更低量化版本 + CUDA_VISIBLE_DEVICES

**原理：** 如果 16K 上下文下 KV cache 显存不够，下载更低量化版本（如 Q3_K_M ~12-14GB），腾出更多显存给长上下文。

**步骤：**
1. 从 HuggingFace 下载 Q3_K_M 或 Q4_0 版本的 qwen3.6 27B GGUF
2. 用 `OLLAMA_NUM_GPU=9999` + `CUDA_VISIBLE_DEVICES=1` 加载
3. 可将上下文提升到 32K+

**可用量化版本及显存占用：**
```
量化版本        文件大小    3090 剩余显存
────────────────────────────────────────
Q4_K_M (~17GB)  17GB      ~3-4GB ✅ (当前)
Q3_K_M (~14GB)  14GB      ~6-7GB ✅✅
Q2_K   (~12GB)  12GB      ~8-9GB ✅✅✅
```

**优点：** 上下文更长、显存余量更大
**缺点：** 量化越低，质量略有下降
**风险：** 低

---

### 方案 C：多 GPU 联合加载（双卡协作）

**原理：** 让 Ollama 同时使用两张 GPU，模型层拆分到 5070 Ti 和 3090。

**步骤：**
1. 设置 `OLLAMA_NUM_GPU=9999`（全部层放入 GPU）
2. 不限制 CUDA_VISIBLE_DEVICES
3. Ollama 自动按显存比例分配层

**显存分配预估：**
```
┌─────────────────────────────┐
│  RTX 5070 Ti (16GB)        │
│  ├── 已有占用 ~14GB        │
│  └── 模型层 ~1-2GB ❗ 紧张  │
├─────────────────────────────┤
│  RTX 3090 (24GB)           │
│  └── 模型层 ~15GB ✅       │
└─────────────────────────────┘
```

**优点：** 理论上可加速（并行计算）
**缺点：** 5070 Ti 已占 14GB，实际可用空间极小；层拆分可能有通信开销
**风险：** ⚠️ 中高（5070 Ti 显存不足可能 fallback 到 CPU）

---

### 方案 D：Ollama 部分卸载（GPU + CPU RAM 混合）

**原理：** 模型大部分层放入 3090，少量层留在 CPU 内存，平衡速度和显存。

**步骤：**
1. `CUDA_VISIBLE_DEVICES=1`
2. 设置 `OLLAMA_NUM_GPU` 为较小的值（如 30），保留部分层在 CPU
3. 利用 64GB 系统内存弥补

**优点：** 显存压力最小，可跑超大上下文
**缺点：** CPU 层推理速度明显慢于 GPU 层
**风险：** 低

---

## 🏆 推荐路线

```
第一步 → 方案 A（CUDA_VISIBLE_DEVICES=1）
         ↓ 验证推理速度 + 显存使用
         ↓ 如果需要更长上下文
第二步 → 方案 B（降级量化 + 提升上下文）
         ↓ 如果还想压榨双卡
第三步 → 方案 C（双卡联合，需先清理 5070 Ti）
```

---

## ✅ 待办

- [ ] 确认 Ollama 当前启动方式（系统服务？手动启动？）
- [ ] 测试方案 A：设置 `CUDA_VISIBLE_DEVICES=1` 重启 Ollama
- [ ] 推理测试：跑一段长 prompt，检查速度 + 显存
- [ ] 确认 16K 上下文是否够用，不够则考虑方案 B
- [ ] 将最终配置写入 Ollama 启动脚本，永久生效
